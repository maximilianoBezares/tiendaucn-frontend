import { NewProductView } from "@/views";

export const metadata = {
  title: "Nuevo Producto",
  description: "Página para crear un nuevo producto",
};

export default function NewProductPage() {
  return <NewProductView />;
}
