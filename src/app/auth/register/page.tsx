import { RegisterView } from "@/views";

export const metadata = {
  title: "Registrarse - Tienda UCN",
  description: "Página para que los usuarios se registren en la plataforma.",
};

export default function RegisterPage() {
  return <RegisterView />;
}